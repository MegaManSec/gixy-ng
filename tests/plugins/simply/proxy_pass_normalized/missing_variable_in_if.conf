location / {
  rewrite ^ $request_uri; # Sets the $1/$uri variable to the raw path
  if ($arg_x) {
    proxy_pass http://127.0.0.1:8000/; # No $1 or $uri used, path normalization risk
  }
}

# Request received by nginx: /%2F
# Request received by backend: /%252F


